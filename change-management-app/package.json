{
  "name": "change-management-app-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start --prefix backend\" \"npm run start --prefix frontend\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "postbuild": "npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "proxy": "https://change-management-backend-df3a3c4ee29f.herokuapp.com/"}
